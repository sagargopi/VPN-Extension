# Backend API URL
REACT_APP_BACKEND_URL=https://vpn-extension-2.onrender.com

# WebSocket URL (if using WebSockets)
REACT_APP_WS_URL=wss://vpn-extension-2.onrender.com/ws

# Development server port (for local development)
PORT=3000

# Webpack dev server settings
WDS_SOCKET_PORT=0  # Disable WebSocket for HMR to prevent conflicts
SKIP_PREFLIGHT_CHECK=true
